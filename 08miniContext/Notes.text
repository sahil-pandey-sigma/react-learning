When you create UserContext, it will store the contexts but you'll need to make UserContextProvider for providing to other components. 

There can be many context like UserContext, EmployeeContext, StudentContext etc.